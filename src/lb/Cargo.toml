[package]
name = "rinhaLb"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
debug = false
incremental = false
